import React, { FC } from 'react';
import Clients from '../../components/Clients';

import Header from '../../components/Header';

const Home: FC = () => <Clients header={Header} />;

export default Home;
